#allows R Studio to read the csv file
arbuthnot <- read.csv("present.csv", header = TRUE)
#allows R Studio to read the csv file
present <- read.csv("present.csv", header = TRUE)
#allows R Studio to read the csv file
present <- read.csv("present.csv", header = TRUE)
#allows R Studio to read the csv file
present <- read.csv("present.csv", header = TRUE)
#allows R Studio to read the csv file
present <- read.csv("present.csv", header = TRUE)
# Setting a working directory to get the proper data
setwd("/Users/raphaelpalacio/Desktop/Emory/Y3S1/QTM100/Lab/LabHomework/LabHw1")
#allows R Studio to read the csv file
present <- read.csv("present.csv", header = TRUE)
# Dimensions of data set
dim(present)
# Names of the columns (variable names)
names(present)
# Years within the data set
present$year
present$propBoys
present$propBoys
arbuthnot$propBoys
arbuthnot$propBoys
# setting the work director
setwd("/Users/raphaelpalacio/Desktop/Emory/Y3S1/QTM100/Lab/labDataSets")
arbuthnot$propBoys
arbuthnot$propBoys
#allows R Studio to read the csv file
arbuthnot <- read.csv("arbuthnot.csv", header = TRUE)
arbuthnot$propBoys
arbuthnot$propBoys
# Proportion using the columns/variables
arbuthnot$propBoys <- arbuthnot$boys / (arbuthnot$boys + arbuthnot$girls)
arbuthnot$propBoys
# Proportion of boys and girls
present$propBoys <- present$boys / (present$boys + present$girls)
present$propBoys
#Plotting the boys over time
plot(x = present$year, y = present$propBoys)
present$propGirls
present$propGirls <- present$girls / (present$boys + present$girls)
present$propGirls
# Plotting the boys over time
plot(x = present$year, y = present$propGirls)
plot(x = present$year, y = present$propBoys)
# Plotting the boys over time
plot(x = present$year, y = present$propBoys, type = "l")
?plot
xlab = "Year", ylab = "Proportion of Boys Born Over Time",
xlab = "Year" ylab = "Proportion of Boys Born Over Time"
title(main = "Proportions of boys over time ", sub = NULL,
xlab = "Year", ylab = "Proportion of Boys Born Over Time")
title(main = "Proportions of boys over time ", sub = NULL,
xlab = NULL, ylab = NULL)
# Plotting the boys over time
plot(x = present$year, y = present$propBoys, type = "l")
title(main = "Proportions of boys over time ", sub = NULL,
xlab = NULL, ylab = NULL)
# How many years did the proportion of boys exceed 52?
present$propBoys > 0.512
# Years within the data set
present$year
# Largest total number of births(?)
which.max(present)
# Largest total number of births(?)
which.max(present$year)
# Largest total number of births(?)
which.max(present$propBoys)
# Largest total number of births(?)
which.max(present$years)
# Largest total number of births(?)
which.max(present$years)
# Largest total number of births(?)
which.max(present$year)
View(present)
# Years within the data set
present$year
present$propBoys > present$propGirls
# Find the year with the largest number of births
max(present$boys+present$girls)
present$total<-present$boys+present$girls
present$total
